# 接力文档 v1.0.207

> 记事便签默认看板视图，最新 tag: v1.0.207

## 当前状态

**v1.0.207**，GitHub Release 三件套齐全（exe + blockmap + latest.yml）。工作区干净，全部已推送。

## 本会话已交付

| 内容 | 说明 |
|------|------|
| 📊 **记事便签默认看板视图** | `client/src/stores/notes.js` 中 `viewMode` 默认值从 `'card'` 改为 `'kanban'`。进入记事便签直接显示按状态分组的看板（待办/进行中/已完成/跟进后续），方便看进度 |

## 需求确认

祥哥：「记事便签进去后能不能默认看板视图，对进度清楚一些」。

## 技术要点

- **仅改默认值 1 行**：`viewMode: 'kanban'`，未做持久化（保持"每次进入默认看板"，符合需求原意）
- `load()` 已按 viewMode 自动切 pageSize（kanban→200 条全量，card→50），改默认值后进入自动加载全量供看板分组
- **搜索/筛选栏仅在卡片视图显示**（Notes.vue `v-if="viewMode === 'card'"`）——默认看板后，要搜索需先切 📋 卡片视图（这是原有设计，未改）
- viewMode 仅在 notes store + Notes.vue 引用，无其他耦合

## 验证证据

- 浏览器实机验证（截图 `notes-kanban-default.png`）：
  1. 进入记事便签 → **默认显示看板**，四列状态计数正确（待办2/进行中1/已完成1/跟进后续1）
  2. 切 📋 卡片 → 搜索/筛选栏出现 ✅
  3. 切回 📊 看板 → 正常 ✅
- 测试数据已清理（本机库恢复原状）

## 待办/未决策（上轮遗留）

- [ ] 导入价格日志是否改（①不生成 ②标记「📥导入初始价」）——祥哥未答复
- [ ] 祥哥 D 盘数据恢复善后确认
- [ ] 客户物料看板视图
- [ ] 记事便签：看板默认后，搜索栏被隐藏——祥哥如觉得不便，可考虑看板模式也加搜索框

## 关键文件

| 文件 | 说明 |
|------|------|
| `client/src/stores/notes.js` | viewMode 默认值 card→kanban |
| `notes-kanban-default.png` | 验证截图 |

相关记忆：[[handoff-v1.0.206]] [[release-after-change]] [[browser-verify-rule]]
